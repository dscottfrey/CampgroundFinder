# Runtime deps. Steps 1–3 of the build plan run on the stdlib alone; these are
# needed for live providers, config, alerts, and (step 5) the web UI.
# Pin camply and re-check app/providers/camply_provider.py when bumping (§13).
# 0.34.2 is the version in samples/camply-main that the adapter was verified against.
camply==0.34.2
PyYAML>=6.0
# `requests` is used directly by the ReserveAmerica and GoingToCamp clients.
# It arrives via camply too, but a direct dependency is declared directly.
# NOT httpx: it is built on the strict h11 parser, and ReserveAmerica ends
# chunked bodies without a terminator. urllib3 tolerates that; strict
# parsers do not. See _fetch_url in app/providers/reserveamerica.py.
requests>=2.31
apprise>=1.8
apscheduler>=3.10
fastapi>=0.115
uvicorn[standard]>=0.30
